{
  "name": "gcs-sidecar-example-app",
  "version": "1.0.0",
  "description": "An example main application in Node.js that reads and writes files on a shared volume synced by the GCS Sidecar.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
